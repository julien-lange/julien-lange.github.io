source "https://rubygems.org"

# Matches what GitHub Pages runs, so a local `bundle exec jekyll serve`
# builds the same output as the deployed site.
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"   # not bundled with Ruby 3.x, needed by `jekyll serve`
